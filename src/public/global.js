export const remote = window.remote;
export const electron = window.electron;
const Store = window.Store;
export const myNotifier = window.notifier

export const memoListLogStore = new Store({"name": "memo-list-log"});
